<html>
 <head>
  <link href="/styles.css" rel="stylesheet" type="text/css">
 </head>
 <body>
  <h1>closing? Auction Board</h1>
  <% var posDisp %>
  <% var curPlayer %>
  <% var nextPlayer %>
  <div>
  <% for (var i = 0; i < BidData.length; i++) { %>
    <% if (BidData[i].secPos) { 
     posDisp = BidData[i].pos + '/' + BidData[i].secPos;
    } else {
     posDisp = BidData[i].pos;
    } %>
    <% curPlayer = posDisp + ' ' + BidData[i].firstName + ' ' + BidData[i].lastName; %>
    <% if ((curPlayer !== nextPlayer) && (!BidData[i].close)) { %>
     </div><div><%= curPlayer %> <%= BidData[i].team %> - <%= BidData[i].bid %> <%= BidData[i].bidTime %>
    <% } %>
    <% nextPlayer = curPlayer %>
  <% } %>
  </div>
 </body>
</html>
